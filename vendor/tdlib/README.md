# TDLib Binary Drop Folder

Place your locally built `tdjson` shared library here.

Expected file names:

- Windows: `tdjson.dll`
- Linux: `libtdjson.so`
- macOS: `libtdjson.dylib`

This folder is intentionally kept out of source control for the binary artifacts themselves.
